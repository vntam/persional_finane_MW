// Auth feature entry: orchestrates login/register/password flows + API calls.
function AuthFeature() {
  return <div className="p-8">Auth screens will live here.</div>;
}

export default AuthFeature;
