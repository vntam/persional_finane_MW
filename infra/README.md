# Infra Notes

- `docker-compose.yml` (root) brings up db + api + web for local dev.
- Add IaC definitions (Terraform, Pulumi, etc.) in this folder when infra work starts.
- Keep environment provisioning scripts (e.g., for staging/prod) here.
